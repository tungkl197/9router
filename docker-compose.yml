services:
  9router:
    build: .
    container_name: 9router
    ports:
      - "20128:20128"
    env_file:
      - .env
    volumes:
      - 9router-data:/app/data
    restart: unless-stopped

volumes:
  9router-data:
